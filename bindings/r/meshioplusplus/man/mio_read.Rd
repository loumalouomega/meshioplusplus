% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meshioplusplus.R
\name{mio_read}
\alias{mio_read}
\alias{mio_reader_supports_options}
\title{Read a mesh file}
\usage{
mio_read(
  path,
  format = NULL,
  points_only = FALSE,
  metadata_only = FALSE,
  arrays = NULL,
  mmap = "auto"
)

mio_reader_supports_options(format)
}
\arguments{
\item{path}{Path to the file.}

\item{format}{Explicit format name, or \code{NULL} to infer it from the extension.}

\item{points_only}{Read geometry only, skipping every data array.}

\item{metadata_only}{Read the header/summary only.}

\item{arrays}{\code{NULL} keeps every data array; a character vector keeps only
the named ones. \code{character(0)} means \emph{no} arrays, which is deliberately a
different request from \code{NULL}.}

\item{mmap}{One of \code{"auto"}, \code{"on"}, \code{"off"}.}
}
\value{
\code{mio_read()} returns a \code{mio_mesh} object.
\code{mio_reader_supports_options()} returns a single logical: whether the
format has a \emph{native} selective-read path. A format without one is still
readable with these options, it is just read in full first.
}
\description{
Read a mesh file
}
\examples{
\dontrun{
m <- mio_read("bracket.msh")
m <- mio_read("bracket.vtu", points_only = TRUE)
}
mio_reader_supports_options("vtu")
}
