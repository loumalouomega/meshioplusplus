% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meshioplusplus.R
\name{mio_extract_surface}
\alias{mio_extract_surface}
\alias{mio_extract_skin}
\alias{mio_attach_quality}
\alias{mio_quality_counts}
\alias{mio_transform}
\alias{mio_clean}
\alias{mio_smooth}
\alias{mio_crop_bbox}
\alias{mio_crop_plane}
\alias{mio_slice}
\alias{mio_gradient}
\alias{mio_estimate_error}
\alias{mio_isosurface}
\alias{mio_merge}
\alias{mio_interpolate}
\alias{mio_undo_green}
\alias{mio_meshes_equal}
\alias{mio_diff}
\alias{mio_stats}
\alias{mio_compute_bandwidth}
\alias{mio_reorder}
\alias{mio_split}
\alias{mio_convert_cells}
\alias{mio_subdivide}
\alias{mio_agglomerate}
\alias{mio_refine}
\alias{mio_decimate}
\alias{mio_partition}
\alias{mio_partition_labels}
\title{Mesh operations}
\usage{
mio_extract_surface(mesh, record_parent_ids = FALSE)

mio_extract_skin(mesh, linearize = FALSE)

mio_attach_quality(mesh)

mio_quality_counts(mesh)

mio_transform(mesh, matrix, rotate_vector_data = FALSE)

mio_clean(
  mesh,
  weld = FALSE,
  atol = 1e-12,
  remove_orphans = TRUE,
  drop_degenerate = TRUE,
  drop_duplicate_cells = TRUE
)

mio_smooth(
  mesh,
  method = "taubin",
  iterations = 10L,
  lambda = -1,
  mu = -0.34,
  fix_boundary = TRUE,
  preserve_features = TRUE,
  feature_angle = 30,
  guard_inversion = TRUE
)

mio_crop_bbox(mesh, lo, hi, mode = "all", record_ids = FALSE)

mio_crop_plane(mesh, point, normal, mode = "all", record_ids = FALSE)

mio_slice(mesh, origin, normal, record_parent_ids = FALSE)

mio_isosurface(
  mesh,
  array,
  isovalues,
  component = -1L,
  record_parent_ids = FALSE
)

mio_gradient(
  mesh,
  array,
  op = "gradient",
  method = "green-gauss",
  location = "cell",
  output = "",
  component = -1L,
  overwrite = FALSE
)

mio_estimate_error(
  mesh,
  array,
  method = "zz",
  marking = "none",
  marking_value = 0,
  output = "",
  marked = "",
  overwrite = FALSE
)

mio_merge(
  meshes,
  weld = FALSE,
  atol = 1e-12,
  source_tag = TRUE,
  data_policy = "intersection",
  drop_duplicate_cells = FALSE
)

mio_interpolate(
  source,
  target,
  method = "nearest",
  arrays = NULL,
  extrapolate = FALSE,
  default_value = 0,
  on_conflict = "error"
)

mio_undo_green(coarse, fine)

mio_meshes_equal(a, b, atol = 0, rtol = 0, unordered = FALSE)

mio_diff(a, b, atol = 0, rtol = 0, unordered = FALSE)

mio_stats(mesh)

mio_compute_bandwidth(mesh)

mio_reorder(mesh, method = "rcm")

mio_split(mesh, by = "type", tag_name = NULL)

mio_convert_cells(mesh, convert_mode, record_parent_ids = FALSE)

mio_subdivide(mesh, record_parent_ids = FALSE)

mio_agglomerate(mesh, target_group_size = 8)

mio_refine(mesh, levels = 1L, record_parent_ids = FALSE)

mio_decimate(
  mesh,
  ratio = -1,
  target_faces = -1,
  max_error = -1,
  placement = "optimal",
  preserve_boundary = TRUE,
  preserve_features = TRUE,
  feature_angle = 30
)

mio_partition(
  mesh,
  nparts,
  method = "auto",
  imbalance = 0.03,
  partition_mode = "eco",
  seed = 0L,
  record_ids = FALSE,
  ghost_layers = 0L,
  weights = NULL
)

mio_partition_labels(
  mesh,
  nparts,
  method = "auto",
  imbalance = 0.03,
  partition_mode = "eco",
  seed = 0L,
  weights = NULL
)
}
\arguments{
\item{mesh, source, target, a, b}{\code{mio_mesh} objects.}

\item{record_parent_ids}{Attach a cell-data array naming each output cell's
source cell.}

\item{linearize}{Emit corner nodes only (triangle/quad output).}

\item{matrix}{A 4x4 affine matrix; point \code{p} maps to \code{M \%*\% c(p, 1)}.}

\item{rotate_vector_data}{Also rotate vector (trailing dim 3) and tensor
(trailing dim 9) point data by the transform's 3x3 linear block.}

\item{weld}{Fuse coincident points within \code{atol}.}

\item{atol}{Absolute coincidence tolerance.}

\item{remove_orphans}{Drop points referenced by no cell.}

\item{drop_degenerate}{Drop degenerate cells.}

\item{drop_duplicate_cells}{Drop exact-duplicate cells.}

\item{method}{For \code{mio_smooth()}, \code{"taubin"} or \code{"laplacian"}; for
\code{mio_reorder()}, \code{"rcm"}, \code{"morton"} or \code{"hilbert"}; for
\code{mio_partition()}, \code{"sfc"}, \code{"kahip"} or \code{"auto"}; for
\code{mio_interpolate()}, \code{"nearest"} or \code{"barycentric"}.}

\item{iterations}{Smoothing iterations.}

\item{lambda}{Relaxation factor in (0, 1). \strong{Negative means "this method's
own default"}: 0.5 for laplacian, 0.33 for taubin.}

\item{mu}{Taubin's un-shrinking factor, which must satisfy \code{mu < -lambda < 0}.}

\item{fix_boundary}{Pin every node on a boundary facet.}

\item{preserve_features}{Pin boundary nodes whose incident facets meet above
\code{feature_angle}.}

\item{feature_angle}{Feature angle in degrees (30 is the \code{vtkFeatureEdges}
convention).}

\item{guard_inversion}{Reject any move that would flip a cell's signed
measure.}

\item{lo, hi}{Bounding-box corners (3 numbers each).}

\item{point, normal, origin}{Plane definition (3 numbers each).}

\item{mode}{\code{"all"} (every node inside) or \code{"any"} (at least one).}

\item{record_ids}{Attach \code{crop:}/\code{partition:} original-id arrays.}

\item{array}{The point-data array to contour.}

\item{isovalues}{One or more level values.}

\item{component}{Component of a multi-component array. For \code{mio_isosurface}
a negative value means the row magnitude; for \code{mio_gradient} it means
\strong{every} component -- deliberately the opposite sentinel.}

\item{op}{For \code{mio_gradient}: \code{"gradient"}, \code{"divergence"} or \code{"curl"}.}

\item{method}{For \code{mio_gradient}: \code{"green-gauss"} or \code{"least-squares"}. For
\code{mio_estimate_error}: only \code{"zz"} exists today; \code{""} selects it.}

\item{location}{For \code{mio_gradient}: \code{"cell"} or \code{"point"}.}

\item{output}{Output array name; \code{""} uses \code{<array>:<op>} for
\code{mio_gradient}, \code{"error:zz"} for \code{mio_estimate_error}.}

\item{marking}{For \code{mio_estimate_error}: \code{"none"} (default), \code{"absolute"},
\code{"fraction"}, or \code{"dorfler"}.}

\item{marking_value}{For \code{mio_estimate_error}: meaning depends on
\code{marking} -- an absolute indicator threshold, a fraction in \code{(0, 1]} of
cells, or the Doerfler bulk fraction theta in \code{(0, 1]}. Ignored for
\code{"none"}.}

\item{marked}{For \code{mio_estimate_error}: the marking array name; \code{""} uses
\code{"error:marked"}. Ignored when \code{marking} is \code{"none"}.}

\item{overwrite}{Replace an existing array of the output name instead of
failing.}

\item{meshes}{A list of \code{mio_mesh} objects.}

\item{source_tag}{Add an integer \code{source_mesh_id} cell-data array.}

\item{data_policy}{\code{"intersection"} (keep keys present in every input) or
\code{"fill"} (union, NaN for missing rows).}

\item{arrays}{Source array names to transfer; \code{NULL} means every source
point-data array.}

\item{extrapolate}{Give a target point outside the source domain the nearest
source value instead of \code{default_value}.}

\item{default_value}{Fill value outside the source domain.}

\item{on_conflict}{\code{"error"}, \code{"overwrite"} or \code{"suffix"}.}

\item{coarse}{For \code{mio_undo_green()}, the mesh a prior \code{mio_refine()} call
was run on.}

\item{fine}{For \code{mio_undo_green()}, that call's output; must carry
\code{refine:cell_id}/\code{refine:parent_id}/\code{refine:level} (i.e. that call must
have used \code{record_hierarchy = TRUE, record_levels = TRUE}).}

\item{rtol}{Relative tolerance.}

\item{unordered}{Match points by spatial proximity instead of by index.}

\item{by}{\code{"type"}, \code{"component"}, or \code{"region"}/\code{"tag"}.}

\item{tag_name}{For \code{by = "region"}, the integer cell-data array name.}

\item{convert_mode}{\code{"linearize"}, \code{"simplexify"} or \code{"elevate"}.}

\item{target_group_size}{Approximate member cells per \code{mio_agglomerate()}
output group; must be at least 1 (\code{1} groups every cell by itself).}

\item{levels}{How many times to apply the refinement templates.}

\item{ratio}{Fraction of faces to KEEP, in (0, 1]; negative = unset.}

\item{target_faces}{Absolute face count to stop at; negative = unset.}

\item{max_error}{Quadric-error ceiling; negative = unset.}

\item{placement}{\code{"optimal"}, \code{"midpoint"} or \code{"endpoint"}.}

\item{preserve_boundary}{Pin boundary vertices during decimation.}

\item{nparts}{Number of partitions.}

\item{imbalance}{KaHIP only: allowed imbalance fraction in (0, 1).}

\item{partition_mode}{KaHIP only: \code{"fast"}, \code{"eco"} or \code{"strong"}.}

\item{seed}{KaHIP only: random seed.}

\item{ghost_layers}{Reserved; must be 0.}

\item{weights}{Name of a scalar numeric cell-data array of per-cell weights.}
}
\value{
A \code{mio_mesh}, or a named list containing one plus the operation's
maps and counters. \code{mio_quality_counts()}, \code{mio_stats()} and \code{mio_diff()}
return named lists; \code{mio_meshes_equal()} a single logical;
\code{mio_compute_bandwidth()} a single number; \code{mio_split()} and
\code{mio_partition()} lists of pieces; \code{mio_partition_labels()} a numeric
vector of part ids.
}
\description{
Computations on a mesh rather than file formats. Each returns a new mesh (or
a list containing one); the input is never modified.
}
\details{
Index maps and permutations are \strong{1-based}, and the C API's \code{-1}
"pruned / absent" sentinel becomes \code{0} -- never a valid 1-based index, so it
stays unambiguous. \code{mio_partition_labels()} is the exception: those are part
\emph{ids}, not indices, so they stay in \code{0:(nparts - 1)}.

The \code{frozen} pin mask of \code{mio_smooth()} and \code{mio_decimate()} is not exposed
across the C ABI, a documented gap shared with the Fortran and Julia
bindings.
}
\examples{
\dontrun{
m <- mio_read("bracket.msh")
surf <- mio_extract_surface(m)
mio_write(surf, "bracket_surface.vtu")
}
}
