% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meshioplusplus.R
\name{mio_num_points}
\alias{mio_num_points}
\alias{mio_point_dim}
\alias{mio_num_cell_blocks}
\alias{mio_num_cells}
\alias{mio_cell_block_info}
\alias{mio_cell_block_type}
\alias{mio_cell_block_types}
\title{Mesh size and cell-block structure}
\usage{
mio_num_points(mesh)

mio_point_dim(mesh)

mio_num_cell_blocks(mesh)

mio_num_cells(mesh)

mio_cell_block_info(mesh, block)

mio_cell_block_type(mesh, block)

mio_cell_block_types(mesh)
}
\arguments{
\item{mesh}{A \code{mio_mesh} object.}

\item{block}{A \strong{1-based} cell-block index.}
}
\value{
\code{mio_num_points()}, \code{mio_point_dim()}, \code{mio_num_cells()} and
\code{mio_num_cell_blocks()} return a single number. \code{mio_cell_block_info()}
returns a list with \code{num_cells}, \code{nodes_per_cell} and \code{is_ragged}.
\code{mio_cell_block_type()} returns the meshio type name;
\code{mio_cell_block_types()} returns one per block.
}
\description{
Mesh size and cell-block structure
}
\details{
A ragged block (polygons or polyhedra of varying size) reports
\code{nodes_per_cell == 0} and \code{is_ragged == TRUE}; its connectivity is not
reachable through the meshio++ C API, so \code{\link{mio_connectivity}} raises
an error for it rather than returning something wrong.
}
\examples{
m <- mio_mesh()
mio_set_points(m, matrix(c(0, 0, 0, 1, 0, 0, 0, 1, 0), nrow = 3))
mio_add_cell_block(m, "triangle", matrix(c(1, 2, 3), nrow = 3))
mio_num_points(m)
mio_cell_block_info(m, 1)
mio_release(m)
}
