% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meshioplusplus.R
\name{mio_repair}
\alias{mio_repair}
\title{Repair a surface mesh's orientation, holes and pinched vertices}
\usage{
mio_repair(
  mesh,
  fix_orientation = TRUE,
  orient_outward = TRUE,
  fill_holes = TRUE,
  split_non_manifold = TRUE,
  max_hole_edges = 10,
  weld_tolerance = 0,
  record_provenance = FALSE
)
}
\arguments{
\item{mesh}{A \code{mio_mesh} (surface only; a volume block is refused naming
\code{extract_surface}, a higher-order one naming \code{linearize}).}

\item{fix_orientation}{Rewind triangles so neighbours agree (\code{TRUE}).}

\item{orient_outward}{Flip closed components with a negative volume (\code{TRUE}).}

\item{fill_holes}{Fan-fill boundary loops (\code{TRUE}).}

\item{split_non_manifold}{Duplicate bowtie vertices (\code{TRUE}).}

\item{max_hole_edges}{Longest loop still filled (\code{10}); \verb{<= 0} means no limit.}

\item{weld_tolerance}{Weld coincident points within this distance first;
\code{0} (the default) skips the weld.}

\item{record_provenance}{Attach \code{repair:parent_point} and \code{repair:hole}.}
}
\value{
A list of \code{mesh}, \code{quality_before}, \code{quality_after} (each a list of
\code{boundary_edges}, \code{non_manifold_edges}, \code{inconsistent_pairs},
\code{degenerate_triangles}, \code{watertight}) and the counters \code{num_flipped},
\code{num_components}, \code{largest_component}, \code{num_oriented_outward},
\code{num_unorientable}, \code{num_vertices_split}, \code{num_holes_detected},
\code{num_holes_filled}, \code{num_holes_skipped}, \code{num_faces_added},
\code{num_points_added}, \code{points_welded}.
}
\description{
Surface repair beyond \code{mio_clean()}: weld (opt-in) -> triangulate (blocks
1:1) -> split bowties (a vertex whose triangle star is edge-disconnected is
duplicated, geometry unchanged) -> orient by the topological half-edge rule
per connected component (two triangles sharing an edge agree iff they
traverse it in opposite directions -- exact, where a normal test fails
across a crease) -> fan-fill boundary loops of at most \code{max_hole_edges}
edges, wound to agree with the surrounding surface -> orient closed
components outward. Adapted from NVIDIA PhysicsNeMo's \code{mesh.repair}.
}
\details{
Lower-dimensional blocks ride along; fill triangles land in one trailing
\code{triangle} block (only when there is one). Non-manifold \emph{edges} are counted,
never split or crossed. The C++ index maps are not exposed on the flat ABI.
}
