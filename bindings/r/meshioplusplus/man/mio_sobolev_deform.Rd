% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meshioplusplus.R
\name{mio_sobolev_deform}
\alias{mio_sobolev_deform}
\title{Sobolev (Helmholtz-filtered) deformation}
\usage{
mio_sobolev_deform(
  mesh,
  array,
  length_scale,
  fixed_points_array = "",
  fix_boundary = FALSE,
  record_filtered = FALSE,
  max_iterations = 128L,
  tolerance = 1e-10
)
}
\arguments{
\item{mesh}{A \code{mio_mesh}.}

\item{array}{Name of the \verb{(n, dim)} point-data displacement array.}

\item{length_scale}{The smoothing length; \code{0} applies the raw field at free points.}

\item{fixed_points_array}{Name of a point-data array whose nonzero entries
pin their point (\code{""} pins nothing this way). Note R writes point data as
\code{Float64}, which this accepts.}

\item{fix_boundary}{Also pin every point on a boundary facet.}

\item{record_filtered}{Attach \code{sobolev:displacement}.}

\item{max_iterations}{Conjugate-gradient cap (\code{128}).}

\item{tolerance}{Relative residual tolerance (\code{1e-10}).}
}
\value{
A list of \code{mesh}, \code{num_iterations}, \code{residual}, \code{converged},
\code{num_fixed}, \code{num_isolated} and \code{max_displacement}.
}
\description{
Solves \verb{(M + l^2 K) u = M d} over the mesh's own P1 finite-element
operators -- \code{K} the stiffness matrix from the simplex edge Gram matrix,
\code{M} the uniform mean lumped vertex mass, matrix-free Jacobi-preconditioned
conjugate gradients -- and moves the points by \code{u}: a screened-Poisson
low-pass filter of the raw displacement \code{d} whose cutoff wavelength is
\code{length_scale}, as in NVIDIA PhysicsNeMo's \code{mesh.sobolev_deform}. Every
top-dimensional block must be a linear simplex (\code{line}, \code{triangle},
\code{tetra}); lower-dimensional blocks ride along. Nothing is pinned by default
(a constant displacement is preserved exactly). Non-convergence is
reported, never raised: the last iterate is returned.
}
