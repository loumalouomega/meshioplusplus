% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meshioplusplus.R
\name{mio_point_data_names}
\alias{mio_point_data_names}
\alias{mio_cell_data_names}
\alias{mio_field_data_names}
\alias{mio_cell_data_num_blocks}
\alias{mio_point_data}
\alias{mio_cell_data}
\alias{mio_field_data}
\title{Named data arrays}
\usage{
mio_point_data_names(mesh)

mio_cell_data_names(mesh)

mio_field_data_names(mesh)

mio_cell_data_num_blocks(mesh, name)

mio_point_data(mesh, name)

mio_cell_data(mesh, name, block = 1)

mio_field_data(mesh, name)
}
\arguments{
\item{mesh}{A \code{mio_mesh} object.}

\item{name}{The array name.}

\item{block}{A \strong{1-based} cell-block index.}
}
\value{
The \verb{*_names()} functions return a character vector in ascending
lexicographic order (identical on every mesh backend).
\code{mio_cell_data_num_blocks()} returns a single number. The accessors return
a numeric vector or array.
}
\description{
A data array shaped \code{(rows, components...)} in the C API comes back with the
dimensions reversed, so a scalar per-point field is a plain vector and a
3-component vector field is a \code{3 x num_points} matrix -- the same memory,
read column-major. The dtype it was stored as is attached as a \code{"dtype"}
attribute; the values themselves are always \code{double}.
}
