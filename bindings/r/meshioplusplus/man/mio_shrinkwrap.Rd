% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meshioplusplus.R
\name{mio_shrinkwrap}
\alias{mio_shrinkwrap}
\title{Project a mesh's points onto a target surface (shrinkwrap)}
\usage{
mio_shrinkwrap(
  mesh,
  target,
  offset = 0,
  max_distance = 0,
  weights = "",
  target_region = "",
  normal_weight = "angle",
  record_distance = FALSE,
  record_closest_cell = FALSE
)
}
\arguments{
\item{mesh}{The \code{mio_mesh} whose points move.}

\item{target}{The surface \code{mio_mesh} to project onto.}

\item{offset}{Signed offset along the feature pseudonormal.}

\item{max_distance}{Leave points farther than this alone; \verb{<= 0} means unlimited.}

\item{weights}{Name of a \verb{(n,)} point-data array on the source: nonzero
selects, a fractional value blends. \code{""} (default) moves every point.}

\item{target_region}{Restrict the target to this named cell region.}

\item{normal_weight}{\code{"angle"} (default) or \code{"area"} vertex-pseudonormal weighting.}

\item{record_distance}{Attach \code{shrinkwrap:distance}.}

\item{record_closest_cell}{Attach \code{shrinkwrap:closest_cell}.}
}
\value{
A list of \code{mesh}, \code{quality} (the target's), \code{num_projected},
\code{num_missed}, \code{num_skipped} and \code{max_displacement}.
}
\description{
\verb{x' = x + w (p + offset n - x)} with \code{p} the closest point on the target
surface and \code{n} the unit pseudonormal of the hit FEATURE (the bisector at a
crease, not the selected triangle's normal) -- one projection, no
iteration, no self-intersection guard, as in NVIDIA PhysicsNeMo's
\code{mesh.shrinkwrap}. Every point of \code{mesh} moves whatever cells it carries;
only \code{target} must be a surface (quads and polygons are fanned). A point
farther than \code{max_distance} from the target is left alone and counted.
Connectivity, data, regions and property sets pass through verbatim.
}
