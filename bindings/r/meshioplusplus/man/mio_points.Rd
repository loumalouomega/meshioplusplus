% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meshioplusplus.R
\name{mio_points}
\alias{mio_points}
\alias{mio_connectivity}
\alias{mio_connectivity_raw}
\title{Point coordinates and cell connectivity}
\usage{
mio_points(mesh)

mio_connectivity(mesh, block = 1)

mio_connectivity_raw(mesh, block = 1)
}
\arguments{
\item{mesh}{A \code{mio_mesh} object.}

\item{block}{A \strong{1-based} cell-block index.}
}
\value{
A numeric matrix: \code{dim x num_points} for \code{mio_points()},
\code{nodes_per_cell x num_cells} for the connectivity accessors.
}
\description{
\code{mio_points()} returns a \code{dim x num_points} matrix. Because R matrices are
column-major and the C core is row-major, that is the \emph{same layout} as the C
API's \code{(num_points, dim)} -- nothing is transposed.
}
\details{
\code{mio_connectivity()} returns \strong{1-based} node indices;
\code{mio_connectivity_raw()} returns the ABI's own \strong{0-based} ones. Both copy:
unlike the Julia and Fortran bindings, R has no zero-copy borrow, so there is
deliberately no \code{_ptr} accessor to imply one.

Values arrive as \code{double} because R has no native 64-bit integer type; that
is exact to 2^53.
}
\examples{
m <- mio_mesh()
mio_set_points(m, matrix(c(0, 0, 0, 1, 0, 0, 0, 1, 0), nrow = 3))
mio_add_cell_block(m, "triangle", matrix(c(1, 2, 3), nrow = 3))
dim(mio_points(m))
mio_connectivity(m, 1) - mio_connectivity_raw(m, 1)
mio_release(m)
}
