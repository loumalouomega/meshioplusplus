% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meshioplusplus.R
\name{mio_regions}
\alias{mio_regions}
\alias{mio_add_region}
\title{Named regions}
\usage{
mio_regions(mesh)

mio_add_region(mesh, name, kind, entries, dim = -1L, tag = -1L)
}
\arguments{
\item{mesh}{A \code{mio_mesh} object.}

\item{name}{The region name.}

\item{kind}{One of \code{"point"}, \code{"cell"}, \code{"side"}.}

\item{entries}{A numeric vector of indices, or a \code{stride x n} matrix
(\code{stride} is 2 for \code{"side"}, 1 otherwise).}

\item{dim}{Topological dimension, or \code{-1} when unspecified.}

\item{tag}{Format-native integer id, or \code{-1} when there is none.}
}
\value{
\code{mio_regions()} returns a list of lists with \code{name}, \code{kind}, \code{dim},
\code{tag} and an \code{entries} matrix. \code{mio_add_region()} returns \code{NULL}
invisibly.
}
\description{
A region is a named group of mesh entities: a Gmsh physical group, an Exodus
block or node/side set, an Abaqus \verb{*NSET}/\verb{*ELSET}/\verb{*SURFACE}, a MED family,
a Kratos SubModelPart.
}
\details{
Entries are \strong{1-based} here, with one exception: for a \code{"side"} region the
second row is a \emph{facet ordinal within the cell type}, not a mesh index, so it
is passed through unshifted. Cell indices are \strong{global and block-major}
(block 1's cells first, then block 2's).
}
\examples{
m <- mio_mesh()
mio_set_points(m, matrix(c(0, 0, 0, 1, 0, 0, 0, 1, 0), nrow = 3))
mio_add_cell_block(m, "triangle", matrix(c(1, 2, 3), nrow = 3))
mio_add_region(m, "inlet", "point", c(1, 2))
length(mio_regions(m))
mio_release(m)
}
