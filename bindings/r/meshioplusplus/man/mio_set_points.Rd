% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meshioplusplus.R
\name{mio_set_points}
\alias{mio_set_points}
\alias{mio_add_cell_block}
\alias{mio_add_point_data}
\alias{mio_append_cell_data}
\alias{mio_add_field_data}
\title{Build a mesh from raw arrays}
\usage{
mio_set_points(mesh, points)

mio_add_cell_block(mesh, cell_type, conn)

mio_add_point_data(mesh, name, data)

mio_append_cell_data(mesh, name, data)

mio_add_field_data(mesh, name, data)
}
\arguments{
\item{mesh}{A \code{mio_mesh} object.}

\item{points}{A \code{dim x num_points} numeric matrix.}

\item{cell_type}{A meshio cell-type name, e.g. \code{"tetra"}.}

\item{conn}{A \code{nodes_per_cell x num_cells} matrix of 1-based node indices.}

\item{name}{The array name.}

\item{data}{A numeric vector or array shaped \code{(components..., rows)}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
All of these \strong{copy} the caller's memory, so the R objects can be reclaimed
as soon as the call returns.
}
\details{
\code{mio_set_points()} takes a \code{dim x num_points} matrix and
\code{mio_add_cell_block()} a \code{nodes_per_cell x num_cells} matrix of \strong{1-based}
node indices; the shift to the core's 0-based indexing happens in the copy.

\code{mio_append_cell_data()} must be called once per cell block, in block order,
after the blocks have been added.
}
\examples{
m <- mio_mesh()
mio_set_points(m, matrix(c(0, 0, 0, 1, 0, 0, 0, 1, 0), nrow = 3))
mio_add_cell_block(m, "triangle", matrix(c(1, 2, 3), nrow = 3))
mio_num_cells(m)
mio_release(m)
}
