% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meshioplusplus.R
\name{mio_xdmf_series}
\alias{mio_xdmf_series}
\alias{mio_xdmf_series_write_points_cells}
\alias{mio_xdmf_series_write_data}
\alias{mio_xdmf_series_finalize}
\alias{mio_xdmf_series_num_steps}
\alias{mio_xdmf_series_release}
\alias{mio_xdmf_series_is_open}
\alias{print.mio_xdmf_series}
\title{Write a transient (time-series) XDMF file}
\usage{
mio_xdmf_series(path, data_format = "HDF", gzip_level = -1L)

mio_xdmf_series_write_points_cells(series, mesh)

mio_xdmf_series_write_data(series, time, mesh)

mio_xdmf_series_finalize(series)

mio_xdmf_series_num_steps(series)

mio_xdmf_series_release(series)

mio_xdmf_series_is_open(series)

\method{print}{mio_xdmf_series}(x, ...)
}
\arguments{
\item{path}{Path of the \code{.xdmf}/\code{.xmf} light-data file to write.}

\item{data_format}{\code{"HDF"} (the default; needs an HDF5-enabled library),
\code{"XML"} (everything inline in the \code{.xdmf}) or \code{"Binary"}. An
unknown format, or \code{"HDF"} against a library built without HDF5, is an
error.}

\item{gzip_level}{Gzip level for \code{"HDF"} datasets; negative (the
default) means no compression. Ignored by the other formats.}

\item{series}{A \code{mio_xdmf_series} object.}

\item{mesh}{A \code{mio_mesh}. \code{mio_xdmf_series_write_points_cells()}
uses only its points and cells; \code{mio_xdmf_series_write_data()} uses only
its \code{point_data}/\code{cell_data}, so a solver can pass the very object
it updates in place. Its cell blocks must match those of the static grid.}

\item{time}{The step's time value.}

\item{x}{A \code{mio_xdmf_series} object.}

\item{...}{Ignored.}
}
\value{
\code{mio_xdmf_series()} returns a new \code{mio_xdmf_series}.
\code{mio_xdmf_series_num_steps()} returns the number of steps written so far
(a \code{double}; R has no native 64-bit integer). The write, finalize and
release functions return \code{NULL} invisibly.
\code{mio_xdmf_series_is_open()} returns a single logical.
}
\description{
The write half of what \code{mio_read(time_step = )} and
\code{mio_read_metadata()$time_values} expose on the read side, and the one
writer \code{mio_write()} cannot express: a series is a \strong{stateful}
multi-call object. The grid is written once with
\code{mio_xdmf_series_write_points_cells()} and each solve appends a cheap
step with \code{mio_xdmf_series_write_data()}.
}
\details{
The \code{.xdmf} light data is \strong{buffered until the series is
finalized}, so the file is only readable after
\code{mio_xdmf_series_finalize()} or \code{mio_xdmf_series_release()}. Heavy
data for \code{data_format = "HDF"} goes to a \verb{<path minus extension>.h5}
\emph{sibling} of the \code{.xdmf}.

The handle is an external pointer with a registered finalizer, exactly like
\code{\link[=mio_mesh]{mio_mesh()}}, so it is released (and finalized) on
garbage collection; \code{mio_xdmf_series_release()} does it immediately and
is idempotent. A write failure during that implicit finalize cannot be
reported from a finalizer, which is why \code{mio_xdmf_series_finalize()}
exists as an explicit call.
}
\examples{
\dontrun{
s <- mio_xdmf_series("simulation.xdmf", data_format = "XML")
mio_xdmf_series_write_points_cells(s, mesh)
for (k in 0:9) {
  mio_xdmf_series_write_data(s, k * 0.1, mesh)
}
mio_xdmf_series_finalize(s)
mio_xdmf_series_release(s)
}
}
