% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meshioplusplus.R
\name{mio_data_drop}
\alias{mio_data_drop}
\alias{mio_data_keep}
\alias{mio_data_rename}
\alias{mio_data_point_to_cell}
\alias{mio_data_cell_to_point}
\alias{mio_data_calc}
\alias{mio_data_condition}
\alias{mio_data_info}
\title{Data operations}
\usage{
mio_data_drop(mesh, location, names, ignore_missing = FALSE)

mio_data_keep(mesh, location, names, ignore_missing = FALSE)

mio_data_rename(mesh, location, from, to)

mio_data_point_to_cell(mesh, names = NULL, suffix = NULL)

mio_data_cell_to_point(mesh, names = NULL, weight = "uniform", suffix = NULL)

mio_data_calc(
  mesh,
  expression,
  output_name,
  location = "point",
  overwrite = FALSE
)

mio_data_condition(
  mesh,
  location,
  names = NULL,
  cond_mode = "clamp",
  lo = 0,
  hi = 1,
  scope = "component",
  nan_policy = "ignore",
  nan_replacement = 0,
  suffix = NULL
)

mio_data_info(mesh)
}
\arguments{
\item{mesh}{A \code{mio_mesh} object.}

\item{location}{One of \code{"point"}, \code{"cell"}, \code{"field"}.}

\item{names}{Array names. \code{NULL} or \code{character(0)} means "every array at that
location" for the averaging and conditioning entry points, and "nothing"
for \code{mio_data_drop()}.}

\item{ignore_missing}{Skip names that do not exist.}

\item{from, to}{Old and new array names.}

\item{suffix}{Write to \code{name + suffix} instead of in place.}

\item{weight}{\code{"uniform"} or \code{"measure"} (weight by cell measure).}

\item{expression}{An elementwise expression over the arrays at \code{location}.
The grammar accepts \verb{+ - * /}, unary minus, parentheses, numeric literals,
array names and the functions \code{abs}, \code{sqrt}, \code{min}, \code{max} and \code{norm}.
Nothing else is evaluated: there is no arbitrary-code path.}

\item{output_name}{Name of the array to create.}

\item{overwrite}{Allow replacing an existing array.}

\item{cond_mode}{\code{"clamp"}, \code{"normalize"} or \code{"standardize"}.}

\item{lo, hi}{Clamp bounds, or the normalize target range.}

\item{scope}{\code{"component"} (each trailing component independently) or
\code{"magnitude"} (rescale whole rows, preserving direction).}

\item{nan_policy}{\code{"ignore"}, \code{"replace"} or \code{"fail"}. Non-finite values are
always excluded from reductions regardless.}

\item{nan_replacement}{Used when \code{nan_policy} is \code{"replace"}.}
}
\value{
A new \code{mio_mesh}, except \code{mio_data_info()}, which returns a list of
per-array summaries.
}
\description{
These act on a mesh's point, cell and field data arrays and \strong{never touch
geometry}: points, connectivity and block order come through bit-identical.
}
\details{
The combined \code{data_manage} of the C++ API is not exposed across the C ABI (a
documented gap); \code{mio_data_drop()}, \code{mio_data_keep()} and
\code{mio_data_rename()} compose to the same effect.
}
