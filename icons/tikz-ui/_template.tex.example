% Copy this to <id>.tex and draw. `make ui` picks up every .tex automatically.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\input{../preamble.tex}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round,
                    >=Stealth, x=1mm, y=1mm, color=iconfg, draw=iconfg]
  % ...
\end{tikzpicture}
\end{document}
